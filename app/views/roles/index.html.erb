<h1>Roles</h1>

<%= link_to 'Crear nuevo rol', new_role_path %>

<table class="listing">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @roles.each do |role| %>
      <tr>
        <td><%= role.role %></td>
        <td><%= link_to 'Ver', role %></td>
        <td><%= link_to 'Editar', edit_role_path(role) %></td>
        <td><%= link_to 'Eliminar', role, method: :delete, data: { confirm: '¿Seguro que deseas eliminar??' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
