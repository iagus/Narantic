<h1>Listado de Urgencias</h1>

<%= link_to 'Nueva urgencia', new_urgency_path %>

<table class="listing">
  <thead>
    <tr class="headers">
      <th>Level</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @urgencies.each do |urgency| %>
      <tr>
        <td><%= urgency.level %></td>
        <td><%= link_to 'Ver', urgency %></td>
        <td><%= link_to 'Editar', edit_urgency_path(urgency) %></td>
        <td><%= link_to 'Eliminar', urgency, method: :delete, data: { confirm: '¿Seguro que deseas eliminar?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
