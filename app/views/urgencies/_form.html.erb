<%= form_for(@urgency, html: { class: 'formikation' }) do |f| %>
  <% if @urgency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@urgency.errors.count, "error") %> prohibited this urgency from being saved:</h2>

      <ul>
      <% @urgency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br>
    <%= f.text_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
