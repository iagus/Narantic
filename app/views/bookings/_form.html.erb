<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :resource_id %>
    <%= f.select :resource_id, options_for_select(Resource.all.map{ |r| [r.name, r.id] }) %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id, :value => current_user.id %>
  </div>
  <div class="field">
    <%= f.label :start %>
    <%= f.date_field :start %>
  </div>
  <div class="field">
    <%= f.label :end %>
    <%= f.date_field :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
