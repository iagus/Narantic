  <div class="right">
    <h1>Listing Bookings</h1>

    <%= link_to 'Crear reserva nueva', new_booking_path %>

    <table class="listing">
      <thead>
        <tr>
          <th>Resource</th>
          <th>User</th>
          <th>Start</th>
          <th>End</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @bookings.each do |booking| %>
          <tr>
            <td><%= booking.resource.name %></td>
            <td><%= booking.user.name %></td>
            <td><%= booking.start %></td>
            <td><%= booking.end %></td>
            <td><%= link_to 'Ver', booking %></td>
            <td><%= link_to 'Editar', edit_booking_path(booking) %></td>
            <td><%= link_to 'Eliminar', booking, method: :delete, data: { confirm: '¿Seguro que deseas eliminar?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
